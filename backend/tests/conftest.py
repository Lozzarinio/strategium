"""
Test fixtures — to be populated in Phase 2.

Will provide:
  - client: TestClient with in-memory SQLite database
  - tournament: a pre-created tournament fixture
  - session_code: the session code from the tournament fixture
"""
